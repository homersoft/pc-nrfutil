--extra-index-url http://pypi.silvair.lan/simple
--trusted-host pypi.silvair.lan

antlib >= 1.1b0; sys_platform == 'win32'
click
crcmod
dbus-python
ecdsa
intelhex
libusb1==1.9.3
# Nordic's official pc-ble-driver-py is archived and never shipped a build newer than
# cp310. This is Silvair's own fork/build (github.com/.../pc-ble-driver-py), published
# to pypi.silvair.lan, verified against Python 3.14. Linux-only for now; win32/darwin
# builds are not published yet.
pc_ble_driver_py_silvair==0.18.0 ; sys_platform == "linux"
piccata
protobuf >=3.17.3, < 4.0.0
pyserial
pyspinel >= 1.0.0a3
pyyaml
tqdm

gobject
PyGObject